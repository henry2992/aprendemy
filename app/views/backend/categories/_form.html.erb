<%= form_for [:backend, @course, @backend_category] do |f| %>
  <% if @backend_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@backend_category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @backend_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    Nombre <br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-success' %> 
  </div>
<% end %>