<header>
		<nav class="navbar navbar-default" role="navigation">
		  <div class="container">
		    <!-- Brand and toggle get grouped for better mobile display -->
		    <div class="navbar-header">
		      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
		        <span class="sr-only">Toggle navigation</span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		      </button>
		      	<a class="navbar-brand aprendemy-logo" href="#">
                    <%= image_tag("logo.png", class: "menu_icon", alt: "Apredemy-logo") %>
		    	</a>
		    </div>

		    <!-- Collect the nav links, forms, and other content for toggling -->
		    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
		      	<ul class="nav navbar-nav navbar-right">
                    <li class="dropdown"><a href="#" class="in-app-anchor dropdown-toggle" data-toggle="dropdown"> Bienvenido <%= current_user.first_name %>
                        <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li class="navbar-mobile-bg">
                                <div class="navbar-content">
                                    <div class="row">
                                        <div class="col-md-5 avatar-container">
                                            <% if (current_user.image != nil) %>
                                                <img src="<%= current_user.image%>" class="img-responsive" alt="Apredemy-foto-usuario">
                                            <% else %>
                                                <%= image_tag("avatar.png", class: "img-responsive", alt: "Apredemy-avatar") %>
                                            <%end%>
                                            <p class="text-center small img-text">
                                                <a href="#">Cambiar Foto de Perfíl</a>
                                            </p>
                                        </div>
                                        <div class="col-md-7 nav-info-container">
                                            <span><%= current_user.first_name %> <%= current_user.last_name %></span>
                                            <p class="text-muted small">
                                                <%= current_user.email %>
                                            </p>
                                            <div class="divider">
                                            </div>
                                            <%= link_to 'Editar perfil', edit_user_registration_path, :class => 'btn btn-primary btn-sm active' %>
                                        </div>
                                    </div>
                                </div>
                                <div class="navbar-footer">
                                    <div class="navbar-footer-content">
                                        <div class="row">
                                            <div class="col-md-12">
                                                <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete, :class => 'btn btn-default btn-sm pull-right'  %>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </li>
                </ul>
		    </div><!-- /.navbar-collapse -->
		  </div><!-- /.container-fluid -->
		</nav>
	</header>