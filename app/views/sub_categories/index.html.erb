<h1>All categories</h1>
<p><%= link_to "New category", new_category_path %></p>

<ul>
<% @sub_categories.each do |sub_category| %>
  <li><%= link_to sub_category.name, category_sub_category_path(sub_category) %></li>
<% end if @sub_categories %>
</ul>
